export enum SessionStatus {
  SCHEDULED = 'scheduled',
  ONGOING = 'ongoing',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum SessionType {
  MEMORIZATION = 'memorization',
  REVIEW = 'review',
  RECITATION = 'recitation',
  EXAM = 'exam',
}
