export enum ExamType {
  ORAL = 'oral',
  WRITTEN = 'written',
  MIXED = 'mixed',
}

export enum ExamStatus {
  SCHEDULED = 'scheduled',
  IN_PROGRESS = 'in_progress',
  GRADED = 'graded',
  CANCELLED = 'cancelled',
}

export enum AssignmentStatus {
  ASSIGNED = 'assigned',
  SUBMITTED = 'submitted',
  REVIEWED = 'reviewed',
  OVERDUE = 'overdue',
}
