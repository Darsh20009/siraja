/**
 * Placeholder for shared Mongoose connection providers/utilities
 * (e.g. multi-tenant connection strategies, transaction helpers).
 * The primary connection is configured in AppModule via MongooseModule.
 */
export {};
