/// Shared Dio-based HTTP client: base URL, interceptors for auth (access
/// token attach + refresh-on-401), and tenant slug injection into the
/// request path/base URL.
///
/// Structure only.
library api_client;
